{
  // Cloudflare Workers config for deploying Calibre (concept demo) as
  // static assets. This is a separate Cloudflare project from the other
  // sites — set "Root directory" to "demo-calibre" when creating the
  // project so this file (and the paths in it) resolve correctly.
  "name": "calibre-demo",
  "compatibility_date": "2026-08-11",
  "assets": {
    "directory": "."
  }
}
